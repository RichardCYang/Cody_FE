# 이 부분은 주석입니다 #

@수신 "시작"{
    # @선언('변수명','값',...) -> 변수 선언    #
    # @해제('변수명') -> 변수 해제            #
    # @참조('변수명') -> 변수 참조            #
    # @호출('함수명','인자값',...) -> 함수 호출 #
    
    @호출('출력','파일3에서 호출되었습니다!')
}